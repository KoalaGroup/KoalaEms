/*
 * ems/events++/strawtest_data.cc
 * 
 * created 2014-Nov-04 PW
 */

#include "config.h"
#include "strawtest_data.hxx"

#if 0
void
clear_channel_data(struct channel_data *cd)
{
    
    cd->raw.clear();
    cd->scaler.clear();
    cd->noise.clear();
}
#endif
