# objects/pi/readout_async/Makefile.in
# $$

CONF = ../../../conf

srcdir = @srcdir@
VPATH = @srcdir@

include $(CONF)/rules.make
include $(CONF)/config.make

cfiles = readout_async.c
ofiles = $(cfiles:.c=.o)
depfiles = $(cfiles)

lib = ../../libobj.a

all: $(lib)

$(lib): $(lib)($(ofiles))
	$(RANLIB) $@

depend: xdepend
.PHONY: depend

###############################################

