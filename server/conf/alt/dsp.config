#include "defaults.inc"

commu = SOCKET
%define DEFPORT 2048

procs = GENERAL DSP

extralflags = -g
%define PROCS

dummyprocs = $(isdummys) $(vardummys) $(pidummys) $(dodummys) $(domdummys) $(immerdummys)
